<%= form_for(current_user.friendships.find_by(friend_id: @user.id),
                                              html: { method: :delete }) do |f| %>
  <%= f.submit "Unfriend", class: "btn btn-secondary",
                           data: { confirm: "You will no longer be friends with #{@user.first_name}!" } %>                                              

<% end %>                  